spring.application.name=Server

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:lamtube}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Cloudinary Configuration
cloudinary.cloud-name=dfnsu6tf4
cloudinary.api-key=662197253735923
cloudinary.api-secret=9A-4UmwbDFeEia2oa1pbP5iPRTQ

# RustFS S3-compatible credentials
rustfs.access-key=rustfsadmin
rustfs.secret-key=rustfsadmin
rustfs.bucket-name=lamtube
rustfs.internal-url=http://localhost:9000
rustfs.public-url-prefix=http://localhost:9000

spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB
